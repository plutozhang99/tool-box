interface:
  display_name: "Phase Review"
  short_description: "Review code, security, coverage, and tests"
  default_prompt: "Use $phase-review to review the current phase diff before I mark it complete."

policy:
  allow_implicit_invocation: true
