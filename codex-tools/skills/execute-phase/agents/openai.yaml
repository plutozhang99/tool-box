interface:
  display_name: "Execute Phase"
  short_description: "Implement, review, commit, and push a phase"
  default_prompt: "Use $execute-phase to complete the next unchecked phase, run $phase-review, commit the result, and push it to GitHub."

policy:
  allow_implicit_invocation: true
